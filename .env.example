## Local development environment
#
# Prisma (SQLite)
# Note: for SQLite, the file path is resolved relative to `prisma/schema.prisma`.
DATABASE_URL="file:./dev.db"

# Auth (JWT signing secret)
# Generate a real secret if you use this beyond local testing.
JWT_SECRET="change-me-in-dev"

